# Modals

## Usage

```tsx
// import
import { useModalStore } from '@/stores/useModalStore';

// use hook
const { openModal, closeModal } = useModalStore();

// open modal using function by providing tsx component to render
openModal(<></>);
```
